export default function GameStatus({ status }) {
    return (
        <div className="inline-block bg-white/10 text-gray-100 px-4 py-2 rounded-full shadow-sm">
            {status}
        </div>
    );
}
